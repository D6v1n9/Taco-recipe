<!DOCTYPE html>
<html>

<head>
  <title>Taco Recipes</title>
  <link rel="stylesheet" type="text/css" href="/styles/main.css">
</head>

<body>
  <h1>Taco Town 🌮</h1>

  <form action="/recipe" method="POST" class="buttons">
    <button type="submit" value="chicken" name="choice" id="0001">🍗</button>
    <button type="submit" value="beef" name="choice" id="0002">🥩</button>
    <button type="submit" value="fish" name="choice" id="0003">🐟</button>
  </form>
  <div id="recipeContainer" class="hidden">

    <!-- If no recipe yet, show: -->
    <% if(locals.name) { %>
      <h2><%=obj.name%></h2>
      <h3>Ingredients:</h3>
    <ul id="ingredientsList">
      
      <li><%=obj.ingredients.protein.name %>, <%=obj.ingredients.protein.preparation%></li>
      <li><%=obj.ingredients.salsa.name %></li>

      <% obj.ingredients.toppings.forEach((topping) => { %>
        <!-- <li>Every other topping</li> -->
        <li><%=topping.quantity%> of <%= topping.name%></li>
      <% }) %>

    </ul>
    <% } else { %>
      <h2>Pick your favourite taco ingredient 👆</h2>
    <% } %>
    <!-- Else, show the recipe name e.g. <h2>Recipe name</h2>-->

    

  </div>
</body>

</html>